const animal =  require('./animal.controllers');
const diario = require('./diario.controllers');
const voluntario = require('./voluntario.controllers');

module.exports={
    animal,
    diario,
    voluntario,
}