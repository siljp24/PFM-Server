const animal = require('./animal.models');
const diario = require('./diario.models');
const voluntario = require('./voluntario.models');
const padrino = require('./padrino.models');

module.exports = {
    animal,
    diario,
    padrino,
    voluntario,
}